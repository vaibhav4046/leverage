node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store
demo/raw/*.mp4
demo/raw/*.webm
.leverage-state/
coverage/
test-results/
playwright-report/

# Regenerated by `next dev` on every start
CLAUDE.md
AGENTS.md
.playwright-mcp/

# The benchmark fixture must fail on a clean checkout. Its src/ is written by
# workers at run time and committing it would make the benchmark measure nothing.
benchmark/forge-app/src/*.js

# Local editor/harness config
.claude/

# The arcade fixture must also fail on a clean checkout. Its src/ is written by
# workers; the *published* build under public/arcade is committed instead, as the
# recorded artefact of a real run.
benchmark/arcade/src/*.js
